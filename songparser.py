class BaseSong:
        def __init__(self, song):
                self.artist = "Unknown"
                self.album = "Unknown"
                self.title = "Unknown"
                self.size = "Unknown"

class BaseLibraryParser:
	def getSongs(self,location):
		return
